{
  "name": "benpearson.dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "publish:vault": "node scripts/publisher.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
