# m10c Sidebar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement an m10c-inspired sidebar layout with profile image, GitHub/LinkedIn links, and accessible themed content pane.

**Architecture:** Keep the custom Hugo theme, but restructure `baseof.html` into an app shell with a fixed visual sidebar and content pane. Use existing CSS variables and theme toggle logic for the content pane while keeping sidebar identity consistent.

**Tech Stack:** Hugo layouts, CSS custom properties, vanilla JavaScript, Node built-in tests.

---

### Task 1: Render Smoke Test

**Files:**
- Create: `scripts/site-render.test.mjs`

- [x] Add a Node test that builds the Hugo site into a temporary directory and asserts the homepage contains the sidebar shell, profile image, GitHub link, LinkedIn link, theme selector, and writing content.
- [x] Run `npm test` and confirm the test fails before implementation.

### Task 2: Sidebar Layout

**Files:**
- Modify: `layouts/_default/baseof.html`
- Modify: `assets/css/main.css`
- Modify: `hugo.toml`
- Create: `static/img/profile.png`

- [x] Add Hugo params for author, description, avatar, and social links.
- [x] Move nav/social/theme controls into the sidebar.
- [x] Add responsive m10c-inspired CSS.
- [x] Store the profile avatar locally.

### Task 3: Verification

**Files:**
- Modify files as needed based on verification output.

- [x] Run `npm test`.
- [x] Run `/tmp/opencode/hugo --minify`.
